name: Agents
on:
  push:
    branches: [feature/agents]
  workflow_dispatch:

jobs:
  setup:
    runs-on: ubuntu-latestatest
    steps:
      - uses: actions/checkout@v4
      - name: Show ready
        run: echo "✅ Agents workflow active"
